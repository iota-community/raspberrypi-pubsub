import * as Promise from 'bluebird';
import { Callback, Provider } from '../../types';
/**
 * @method createBroadcastBundle
 *
 * @memberof module:core
 *
 * @param {Provider} provider - Network provider
 *
 * @return {function} {@link #module_core.broadcastBundle `broadcastBundle`}
 */
export declare const createBroadcastBundle: (provider: Provider) => (tailTransactionHash: string, callback?: Callback<ReadonlyArray<string>> | undefined) => Promise<ReadonlyArray<string>>;
