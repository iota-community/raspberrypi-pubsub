import * as Promise from 'bluebird';
import { Callback, Provider } from '../../types';
/**
 * @method createBroadcastTransactions
 *
 * @memberof module:core
 *
 * @param {Provider} provider - Network provider
 *
 * @return {function} {@link #module_core.broadcastTransactions `broadcastTransactions`}
 */
export declare const createBroadcastTransactions: ({ send }: Provider) => (trytes: ReadonlyArray<string>, callback?: Callback<ReadonlyArray<string>> | undefined) => Promise<ReadonlyArray<string>>;
