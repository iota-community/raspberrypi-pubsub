import * as Promise from 'bluebird';
import { Callback, Provider } from '../../types';
export interface CheckConsistencyOptions {
    rejectWithReason?: boolean;
}
/**
 * @method createCheckConsistency
 *
 * @memberof module:core
 *
 * @param {Provider} provider - Network provider
 *
 * @return {function} {@link #module_core.checkConsistency `checkConsistency`}
 */
export declare const createCheckConsistency: ({ send }: Provider) => (transactions: string | ReadonlyArray<string>, options?: CheckConsistencyOptions | undefined, callback?: Callback<boolean> | undefined) => Promise<boolean>;
