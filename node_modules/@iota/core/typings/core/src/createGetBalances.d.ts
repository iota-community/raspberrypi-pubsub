import * as Promise from 'bluebird';
import { Balances, Callback, Provider } from '../../types';
/**
 * @method createGetBalances
 *
 * @memberof module:core
 *
 * @param {Provider} provider - Network provider
 *
 * @return {function} {@link #module_core.getBalances `getBalances`}
 */
export declare const createGetBalances: ({ send }: Provider) => (addresses: ReadonlyArray<string>, threshold: number, tips?: ReadonlyArray<string> | undefined, callback?: Callback<Balances> | undefined) => Promise<Balances>;
