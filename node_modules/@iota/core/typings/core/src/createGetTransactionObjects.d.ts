import * as Promise from 'bluebird';
import { Callback, Provider, Transaction } from '../../types';
/**
 * @method createGetTransactionObjects
 *
 * @memberof module:core
 *
 * @param {Provider} provider - Network provider
 *
 * @return {Function} {@link #module_core.getTransactionObjects `getTransactionObjects`}
 */
export declare const createGetTransactionObjects: (provider: Provider) => (hashes: ReadonlyArray<string>, callback?: Callback<ReadonlyArray<Transaction>> | undefined) => Promise<ReadonlyArray<Transaction>>;
