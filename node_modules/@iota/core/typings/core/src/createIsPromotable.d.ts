import * as Promise from 'bluebird';
import { Callback, Provider } from '../../types';
export declare const isAboveMaxDepth: (attachmentTimestamp: number, depth?: number) => boolean;
/**
 *
 * @method createIsPromotable
 *
 * @memberof module:core
 *
 * @param {Provider} provider - Network provider
 *
 * @param {number} [depth=6] - Depth up to which promotion is effective.
 *
 * @return {function} {@link #module_core.isPromotable `isPromotable`}
 */
export declare const createIsPromotable: (provider: Provider, depth?: number) => (tail: string, callback?: Callback<boolean> | undefined) => Promise<boolean>;
