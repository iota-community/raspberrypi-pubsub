import * as Promise from 'bluebird';
import { Callback, Provider } from '../../types';
/**
 * @method createRemoveNeighbors
 *
 * @memberof module:core
 *
 * @param {Provider} provider - Network provider
 *
 * @return {function} {@link #module_core.removeNeighbors `removeNeighbors`}
 */
export declare const createRemoveNeighbors: ({ send }: Provider) => (uris: ReadonlyArray<string>, callback?: Callback<number> | undefined) => Promise<number>;
