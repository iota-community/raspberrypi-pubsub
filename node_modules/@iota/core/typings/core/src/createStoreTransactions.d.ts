import * as Promise from 'bluebird';
import { Callback, Provider } from '../../types';
/**
 * @method createStoreTransactions
 *
 * @memberof module:core
 *
 * @param {Provider} provider - Network provider
 *
 * @return {function} {@link #module_core.storeTransactions `storeTransactions`}
 */
export declare const createStoreTransactions: ({ send }: Provider) => (trytes: ReadonlyArray<string>, callback?: Callback<ReadonlyArray<string>> | undefined) => Promise<ReadonlyArray<string>>;
