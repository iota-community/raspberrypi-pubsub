import * as Promise from 'bluebird';
import { Callback, Provider, Transaction } from '../../types';
/**
 * @method createTraverseBundle
 *
 * @memberof module:core
 *
 * @param {Provider} provider
 *
 * @return {function} {@link #module_core.traverseBundle `traverseBundle`}
 */
export declare const createTraverseBundle: (provider: Provider) => (trunkTransaction: string, bundle?: ReadonlyArray<Transaction>, callback?: Callback<ReadonlyArray<Transaction>> | undefined) => Promise<ReadonlyArray<Transaction>>;
