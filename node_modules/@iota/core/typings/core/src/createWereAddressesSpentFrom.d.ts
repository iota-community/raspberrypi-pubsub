import * as Promise from 'bluebird';
import { BaseCommand, Callback, Hash, Provider } from '../../types';
export interface WereAddressesSpentFromCommand extends BaseCommand {
    command: string;
    readonly addresses: ReadonlyArray<Hash>;
}
export interface WereAddressesSpentFromResponse {
    readonly states: ReadonlyArray<boolean>;
}
export declare const createWereAddressesSpentFrom: ({ send }: Provider, caller?: string | undefined) => (addresses: ReadonlyArray<string>, callback?: Callback<ReadonlyArray<boolean>> | undefined) => Promise<ReadonlyArray<boolean>>;
